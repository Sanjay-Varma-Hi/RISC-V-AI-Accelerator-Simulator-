# RISC-V AI Accelerator Simulator - Python Dependencies
# These are the core dependencies needed for Phase 1 setup

# Core ML Framework
tinygrad>=0.8.0

# Scientific Computing
numpy>=1.21.0
scipy>=1.7.0

# Data Processing
pandas>=1.3.0

# Visualization (optional, for debugging)
matplotlib>=3.5.0
seaborn>=0.11.0

# Testing and Development
pytest>=6.0.0
pytest-cov>=2.12.0

# Jupyter (optional, for interactive development)
jupyter>=1.0.0
ipykernel>=6.0.0

# Performance Profiling (optional)
memory-profiler>=0.60.0
line-profiler>=3.3.0

# Note: TinyGrad will automatically install its own dependencies
# including PyTorch, torchvision, etc. when installed with pip install -e .
